<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- LSPosed / Xposed 模块声明 -->
    <!-- scope: 仅作用于小米传送门 -->
    <meta-data
        android:name="xposedmodule"
        android:value="true" />

    <meta-data
        android:name="xposeddescription"
        android:value="小米传送门增强：保存格式 PNG→JPEG + 修复保存竞态 Bug" />

    <meta-data
        android:name="xposedminversion"
        android:value="82" />

    <!-- 可选：LSPosed 专属 scope 配置 -->
    <!-- 直接在 LSPosed 管理器中手动勾选 com.miui.contentextension 即可 -->

    <application
        android:label="Taplus 传送门增强"
        android:icon="@android:mipmap/sym_def_app_icon"
        android:hasCode="true">

        <!-- LSPosed v1.9+ 支持无 Activity 模块 -->
        <!-- 不需要 Activity，LSPosed 直接加载入口类 -->

    </application>
</manifest>
